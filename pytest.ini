# pytest.ini - Pytest configuration for the Signal bot project.
[pytest]
filterwarnings =
    ignore:.*audioop.*is deprecated.*:DeprecationWarning
asyncio_default_fixture_loop_scope = function
asyncio_mode = auto
markers = asyncio

# End of pytest.ini